1. Download & Unzip

2. Copy Guinaifen into grub themes directory
sudo cp -r Guinaifen /usr/share/grub/themes

3. Edit grub file
sudo vim /etc/default/grub

4. Add the theme to the bottom of the text file
GRUB_THEME="/usr/share/grub/themes/Guinaifen/theme.txt"

5. Update grub
sudo grub-mkconfig -o /boot/grub/grub.cfg


git clone https://github.com/catppuccin/grub.git
cd grub/src/
sudo cp -r catppuccin-macchiato-grub-theme /usr/share/grub/themes
sudo vim /etc/default/grub
## Add the theme to the bottom of the text file
GRUB_THEME="/usr/share/grub/themes/catppuccin-macchiato-grub-theme/theme.txt"
##update grub 
sudo grub-mkconfig -o /boot/grub/grub.cfg

